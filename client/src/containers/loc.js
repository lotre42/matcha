
import React, { Component } from 'react'
import axios from 'axios'

class loc extends Component {
    render () {
        var ip = false;
        // axios.get(`http://adresseduserviceip.com`).then((response) =>{ console.log(response)})
        //On suppose que l'adresse IP est stockée avec la clé ip. Regardez les exemples fournis par les services pour savoir quelle clé correspond à l'adresse IP
        return (
            <div>
                C'est cho ca brule
            </div>
        )
    }
}

export default loc

